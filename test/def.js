var assert = require('assert');
    it('should return -1 when the value is not present', function(done){
      setTimeout(function() {
        assert.equal(1, 10);
        done();
      }, 1000);
    })

